<div class="modal fade" tabindex="-1" role="dialog" id="exemption-modal" aria-hidden="true">
  <div class="modal-dialog modal-md">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Fechar</span></button>
        <h4 class="modal-title">Dispensa</h4>
      </div>
      <%= simple_form_for :avaliation_exemption, url: exempt_students_daily_note_path, remote: true, html: { class: 'smart-form' } do |f| %>
      <%= hidden_field_tag 'exemption_students_ids' %>
      <%= hidden_field_tag 'exemption_avaliation_id', @daily_note.avaliation.id%>
      <div class="modal-body row textarea">
        <div class="col col-sm-12">
          Motivo:<abbr title="obrigatório">*</abbr>
          <%= text_area_tag :reason %>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Cancelar</button>
        <button type="submit" class=" btn btn-primary" data-disable-with='Dispensando...'>Dispensar</button>
      </div>
      <% end %>
    </div>
  </div>
</div>
