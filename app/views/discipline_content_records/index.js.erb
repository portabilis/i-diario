$('#resources-tbody').replaceWith('<%= escape_javascript(render('resources')) %>');
$('#pagination-tfoot').html(
  "<%=
    escape_javascript(render(partial: 'shared/pagination', locals: { records: @discipline_content_records }))
  %>"
);
