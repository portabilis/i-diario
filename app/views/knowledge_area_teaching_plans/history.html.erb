<div class="widget-body">
  <% if resource.all_audits.exists? %>
    <div class="table-responsive">
      <table class="table table-bordered table-only-inner-bordered" id="history">
        <thead>
          <tr>
            <th>Ação</th>
            <th>Alterações</th>
            <th>Autor</th>
            <th>Data</th>
          </tr>
        </thead>
        <tbody>
          <legend> Plano de ensino </legend>
          <% resource.teaching_plan.all_audits.each do |audit| %>
            <%= render 'knowledge_area_teaching_plans/history/history', record: AuditDecorator.new(audit) %>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="alert alert-warning alert-dismissable">
      Nenhuma alteração realizada.
    </div>
  <% end %>