$('#resources-tbody').replaceWith('<%= escape_javascript(render('resources')) %>');
$('#pagination-tfoot').html(
  "<%= escape_javascript(render(partial: 'shared/pagination', locals: { records: @observation_diary_records })) %>"
);
