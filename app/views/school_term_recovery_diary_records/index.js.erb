$('#resources-tbody').replaceWith('<%= escape_javascript(render('resources')) %>');
$('#pagination-tfoot').html(
  "<%= escape_javascript(render(partial: 'shared/pagination', locals: { records: @school_term_recovery_diary_records })) %>"
);
