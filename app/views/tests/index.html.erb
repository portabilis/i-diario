<div class="widget-body no-padding">
  <div class="table-responsive">
    <table class="table table-bordered table-striped table-condensed table-hover" id="resources">
      <thead>
        <tr>
          <th><%= Test.human_attribute_name :unity %></th>
          <th><%= Test.human_attribute_name :classroom %></th>
          <th><%= Test.human_attribute_name :discipline %></th>
          <th><%= Test.human_attribute_name :test_date %></th>
          <th><%= Test.human_attribute_name :class_number %></th>
          <th><%= Test.human_attribute_name :description %></th>
          <td class="actions" style="width: 190px;"><%= link_to t('.new_html'), new_test_path, class: "btn btn-primary" %></td>
        </tr>
      </thead>

      <%= render 'resources' %>
      <tfoot id="paginator" class="remote">
        <td colspan="7">
          <div id="page_entries_info" class="pull-left hidden-xs">
            <%= page_entries_info @tests %>
          </div>
          <div class="pull-right">
            <%= paginate @tests %>
          </div>
        </td>
      </tfoot>
    </table>
  </div>
</div>
